{
  "name": "helloAPI",
  "version": "1.0.0",
  "description": "API Spec Explorer",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "cli": "pnpm --filter cli start"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.1.2",
      "onFail": "download"
    }
  }
}